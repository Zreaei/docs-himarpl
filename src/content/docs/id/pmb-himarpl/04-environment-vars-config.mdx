---
title: Environment Variables dan Konfigurasi
description: Pelajari cara mengkonfigurasi environment variables dan konfigurasi lainnya dalam proyek website PMB HIMARPL.
---

Mengelola environment variables dalam proyek kami adalah tugas penting, tetapi juga bisa menantang. Proyek ini menggunakan environment variables untuk manajemen konfigurasi, memanfaatkan [t3-env](https://github.com/t3-oss/t3-env) untuk environment variables yang type-safe dengan validasi runtime. Ketika Anda menambahkan environment variables baru, Anda perlu memperbarui file `.env.example` dan file `src/env.js` untuk memastikan semuanya berfungsi dengan benar.

## File Konfigurasi

### 1. Skema Environment (`src/env.js`)

File `env.js` mendefinisikan skema dan aturan validasi untuk semua environment variables menggunakan Zod. Dibagi menjadi tiga bagian utama:

```javascript title="src/env.js"
import { z } from "zod";
import { createEnv } from "@t3-oss/env-nextjs";

export const env = createEnv({
  /**
   * Specify your server-side environment variables schema here. This way you can ensure the app
   * isn't built with invalid env vars.
   */
  server: {
    DATABASE_URL: z
      .string()
      .url()
      .refine(
        (str) => !str.includes("YOUR_MYSQL_URL_HERE"),
        "You forgot to change the default URL",
      ),
    NODE_ENV: z
      .enum(["development", "test", "production"])
      .default("development"),
    BASE_URL: z.string().url(),
    BASE_PATH: z.string().optional(),
  },

  /**
   * Specify your client-side environment variables schema here. This way you can ensure the app
   * isn't built with invalid env vars. To expose them to the client, prefix them with
   * `NEXT_PUBLIC_`.
   */
  client: {
    // NEXT_PUBLIC_CLIENTVAR: z.string(),
    NEXT_PUBLIC_POSTHOG_KEY: z.string(),
    NEXT_PUBLIC_POSTHOG_HOST: z.string().url(),
  },

  /**
   * You can't destruct `process.env` as a regular object in the Next.js edge runtimes (e.g.
   * middlewares) or client-side so we need to destruct manually.
   */
  runtimeEnv: {
    DATABASE_URL: process.env.DATABASE_URL,
    NODE_ENV: process.env.NODE_ENV,
    BASE_URL: process.env.NEXT_PUBLIC_BASE_URL
      ? process.env.NEXT_PUBLIC_BASE_URL
      : process.env.NEXT_PUBLIC_VERCEL_URL
        ? `https://${process.env.NEXT_PUBLIC_VERCEL_URL}`
        : "https://pmb.himarpl.com",
    // eslint-disable-next-line @typescript-eslint/prefer-nullish-coalescing
    BASE_PATH: process.env.NEXT_PUBLIC_BASE_PATH || "",
    NEXT_PUBLIC_POSTHOG_KEY: process.env.NEXT_PUBLIC_POSTHOG_KEY,
    NEXT_PUBLIC_POSTHOG_HOST: process.env.NEXT_PUBLIC_POSTHOG_HOST,
  },
  /**
   * Run `build` or `dev` with `SKIP_ENV_VALIDATION` to skip env validation. This is especially
   * useful for Docker builds.
   */
  skipValidation: !!process.env.SKIP_ENV_VALIDATION,
  /**
   * Makes it so that empty strings are treated as undefined.
   * `SOME_VAR: z.string()` and `SOME_VAR=''` will throw an error.
   */
  emptyStringAsUndefined: true,
});
```

### 2. File Environment (`.env`)

Buat file `.env` di root proyek dengan nilai khusus environment Anda:

```bash
# Konfigurasi Database
DATABASE_URL="your-database-url"

# Konfigurasi Base URL
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
NEXT_PUBLIC_BASE_PATH=""
NEXT_BASE_URL="http://localhost:3000"

# Analitik PostHog
NEXT_PUBLIC_POSTHOG_KEY="your-posthog-key"
NEXT_PUBLIC_POSTHOG_HOST="https://us.i.posthog.com"
```

## Fitur Utama

- **Type Safety**: Semua environment variables divalidasi saat build dan runtime
- **Validasi Runtime**: Memastikan aplikasi Anda tidak dibangun dengan env vars yang tidak valid
- **Keamanan Client-Side**: Pemisahan yang jelas antara variabel server dan client
- **Nilai Default**: Fallback untuk variabel tertentu ketika tidak ditentukan
- **Penanganan String Kosong**: String kosong diperlakukan sebagai undefined secara default

## Panduan Penggunaan

1. **Variabel Server-Side**:
   - Akses menggunakan `env.VARIABLE_NAME`
   - Tidak pernah diekspos ke client
   - Contoh: `env.DATABASE_URL`

2. **Variabel Client-Side**:
   - Harus diawali dengan `NEXT_PUBLIC_`
   - Dapat diakses dalam kode server dan client
   - Contoh: `env.NEXT_PUBLIC_POSTHOG_KEY`

3. **Development**:
   - Buat file `.env` lokal berdasarkan `.env.example`
   - Jangan pernah commit `.env` ke version control
   - Perbarui skema `env.js` saat menambahkan variabel baru

## Contoh Penggunaan

```javascript title="src/server/db.ts" {1, 7, 16}
import { env } from "@/env";
import { PrismaClient } from "@prisma/client";

const createPrismaClient = () =>
  new PrismaClient({
    log:
      env.NODE_ENV === "development" ? ["query", "error", "warn"] : ["error"],
  });

const globalForPrisma = globalThis as unknown as {
  prisma: ReturnType<typeof createPrismaClient> | undefined;
};

export const db = globalForPrisma.prisma ?? createPrismaClient();

if (env.NODE_ENV !== "production") globalForPrisma.prisma = db;

```

Anda dapat mengakses environment variables dalam kode Anda seperti ini:

```javascript
import { env } from "@/env.js";

// Penggunaan server-side
const dbUrl = env.DATABASE_URL;

// Penggunaan client-side
const posthogKey = env.NEXT_PUBLIC_POSTHOG_KEY;
```
